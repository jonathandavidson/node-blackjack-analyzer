const Card = require('../Card');
const Hand = require('../Hand');

const actions = {
  doubleDown: 'double down',
  hit: 'hit',
  stand: 'stand',
  surrender: 'surrender'
}

module.exports = {
  actions
}
